name: "Test Forkcast Dispatch"

on:
  workflow_dispatch:

jobs:
  test-dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Send dispatch to Forkcast
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.FORKCAST_DISPATCH_TOKEN }}
          script: |
            await github.rest.repos.createDispatchEvent({
              owner: 'dionysuzx',
              repo: 'forkcast',
              event_type: 'pm-assets-updated'
            });
            console.log('Dispatch sent to dionysuzx/forkcast');
